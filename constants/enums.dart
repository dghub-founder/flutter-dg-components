enum ConnectionType { wifi, mobile }

enum SaveType { string, int, bool, double }

enum LayoutType { glassView }

enum ScrollToHideAnimationType { upToDown, downToUp, leftToRight, rightToLeft }

enum ShapeBorderType { circle, beveled, stadium }

enum AppType { movie, shop }
